# Remove anything that contains password data (important)
bak/config/*
run/nodes/*
run/config/*.json 


# Remove utils config (scripts)
etc/utils/.config

# Remove notes (due to presence of mainnet addresses)
etc/notes/mainnet.notes
